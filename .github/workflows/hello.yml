name: My First Workflow

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello Gagan! Your first GitHub Action is running."

      - name: Show Current Directory
        run: pwd

      - name: List Files
        run: ls -la